{
  "name": "substruct-ui-mono",
  "version": "1.0.0",
  "description": "Monorepo for Substruct UI",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jsmmth",
  "devDependencies": {
    "prettier": "^3.0.1",
    "typescript": "^5.0.2",
    "turbo": "^1.5.6"
  }
}
